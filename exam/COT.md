First I Breakdown the problem by scenario, task, then what I need to print. Then i proceed by cleaning the data by using pandas .strip() and and drop duplicates for deduplication, and i parse the scores because currently they are string. i sort the scores using numpy.mean as lambda key to sort. and i create the two custom exceptions invalid score error that raises everytime the score is less than 0 or more than 100. and student locked error when they attempt to add a score after the record has been finalized if is locked is true and they attempt to add then the exception triggers. then i create the class student with name, and scores. after that i create the methods first method is lock if they are finish adding the student record it becomes true.  for add core method they can add ascore as long as locked = false otherwise the custome exceptions is triggered. for the average method the mean from numpy is used. then i implement str and repr. 
